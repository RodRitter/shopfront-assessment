import React from "react";

type ProductGridTypes = {
    children?: React.ReactNode;
};

const ProductGrid = ({ children }: ProductGridTypes) => {
    return <div>ProductGrid</div>;
};

export default ProductGrid;
